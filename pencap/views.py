from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def mowa(request):
    return HttpResponse("<h1 b>mowa i need pencap </h1")
def mor(request):
    return HttpResponse("i will provide that to u")